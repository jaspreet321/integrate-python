a = {1,2,3,4,5,6,7,1,5,6} #just declaring some numnbers to be assumed in varialbe 'a' with curly braces

print(a) #this print command is expected to print all the numbers under variable 'a', but remeber the values are in curly-braces, which makes them 'SETS', then this command will only print uniques values under 'a', no duplicates
